using RPG.Combact;
using UnityEngine;

namespace RPG.Combact
{
    public class Fighter : MonoBehaviour
    {
        public void Attack(CombatTarget target)
        {
            print("take that yo small pp");
        }
    }
}
